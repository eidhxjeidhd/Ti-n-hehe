-- discord.gg/tboyroblox

-- Loader Screen Cao Cấp
shared.LoaderTitle = "gà gaming Hub - VIP Edition"
shared.LoaderColors = {
    Main = Color3.fromRGB(10, 10, 20),
    Topic = Color3.fromRGB(200, 200, 200),
    Title = Color3.fromRGB(255, 215, 0),
    LoaderBackground = Color3.fromRGB(30, 30, 40),
    LoaderSplash = Color3.fromRGB(255, 215, 0)
}

-- Tạo loader đơn giản
local screenGui = Instance.new("ScreenGui", game.CoreGui)
local mainFrame = Instance.new("Frame", screenGui)
mainFrame.Size = UDim2.new(0, 400, 0, 150)
mainFrame.Position = UDim2.new(0.5, -200, 0.5, -75)
mainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 20)
mainFrame.BorderSizePixel = 0

local UICorner = Instance.new("UICorner", mainFrame)
UICorner.CornerRadius = UDim.new(0, 12)

local title = Instance.new("TextLabel", mainFrame)
title.Text = "gà gaming Hub - VIP Edition"
title.TextColor3 = Color3.fromRGB(255, 215, 0)
title.Size = UDim2.new(1, 0, 0, 40)
title.Position = UDim2.new(0, 0, 0, 20)
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.TextSize = 20

local loadingBar = Instance.new("Frame", mainFrame)
loadingBar.Size = UDim2.new(0.8, 0, 0, 20)
loadingBar.Position = UDim2.new(0.1, 0, 0.6, 0)
loadingBar.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
loadingBar.BorderSizePixel = 0

local progressBar = Instance.new("Frame", loadingBar)
progressBar.Size = UDim2.new(0, 0, 1, 0)
progressBar.BackgroundColor3 = Color3.fromRGB(255, 215, 0)
progressBar.BorderSizePixel = 0

-- Hiệu ứng loading
for i = 1, 100 do
    progressBar.Size = UDim2.new(i/100, 0, 1, 0)
    wait(0.03)
end

wait(0.5)
screenGui:Destroy()

-- Load Fluent UI
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

-- Tạo Window Cao Cấp
local Window = Fluent:CreateWindow({
    Title = "gà gaming Hub",
    SubTitle = "VIP Edition", 
    TabWidth = 170,
    Size = UDim2.fromOffset(620, 450),
    MinimizeKey = Enum.KeyCode.F9,
    Theme = "Darker",
    Acrylic = true,
    Mica = true,
})

-- Theme Vàng Gold Cao Cấp
Fluent:ApplyThemeOption("Accent", Color3.fromRGB(255, 215, 0))
Fluent:ApplyThemeOption("WindowBackgroundColor", Color3.fromRGB(10, 10, 20))

-- Tabs với icon
local Tabs = {
    Home = Window:AddTab({Title = "🏠 Thông Tin"}),
    Main = Window:AddTab({Title = "⚔️ Cày Auto"}),
    Sea = Window:AddTab({Title = "🌊 Sự Kiện"}),
    ITM = Window:AddTab({Title = "🎒 Vật Phẩm"}),
    Setting = Window:AddTab({Title = "⚙️ Cài Đặt"}),
    Stats = Window:AddTab({Title = "📊 Chỉ Số"}),
    Player = Window:AddTab({Title = "👤 Người Chơi"}),
    Teleport = Window:AddTab({Title = "📍 Dịch Chuyển"}),
    Fruit = Window:AddTab({Title = "🍎 Trái Ác Quỷ"}),
    Raid = Window:AddTab({Title = "🔥 Tập Kích"}),
    Race = Window:AddTab({Title = "🏹 Tộc"}),
    Shop = Window:AddTab({Title = "🏪 Cửa Hàng"}),
    Misc = Window:AddTab({Title = "🔧 Khác"})
}

local Options = Fluent.Options

-- Tab Thông Tin
Tabs.Home:AddButton({
    Title = "Discord Server",
    Description = "Tham gia cộng đồng",
    Callback = function()
        setclipboard("https://discord.gg/tboyroblox")
    end
})

Tabs.Home:AddParagraph({
    Title = "gà gaming Hub VIP",
    Content = "Phiên bản cao cấp với giao diện vàng gold"
})

-- Tab Cày Auto
Tabs.Main:AddSection("Cài Đặt Chính")

local DropdownWeapon = Tabs.Main:AddDropdown("WeaponSelect", {
    Title = "Vũ Khí",
    Values = {"Melee", "Sword", "Blox Fruit"},
    Default = "Melee",
})

DropdownWeapon:OnChanged(function(value)
    _G.SelectWeapon = value
end)

local ToggleLevel = Tabs.Main:AddToggle("AutoFarm", {
    Title = "Cày Cấp",
    Default = false,
})

ToggleLevel:OnChanged(function(value)
    _G.AutoLevel = value
end)

local ToggleBoss = Tabs.Main:AddToggle("AutoBoss", {
    Title = "Đánh Boss",
    Default = false, 
})

ToggleBoss:OnChanged(function(value)
    _G.AutoBoss = value
end)

-- Tab Vật Phẩm
Tabs.ITM:AddSection("Nguyên Liệu")

local MaterialList = {"Scrap Metal", "Leather", "Magma Ore", "Fish Tail"}
local DropdownMaterial = Tabs.ITM:AddDropdown("MaterialSelect", {
    Title = "Chọn Nguyên Liệu",
    Values = MaterialList,
    Default = "Scrap Metal",
})

DropdownMaterial:OnChanged(function(value)
    _G.SelectMaterial = value
end)

local ToggleMaterial = Tabs.ITM:AddToggle("FarmMaterial", {
    Title = "Cày Nguyên Liệu",
    Default = false,
})

ToggleMaterial:OnChanged(function(value)
    _G.AutoMaterial = value
end)

-- Tab Cài Đặt
Tabs.Setting:AddSection("Tùy Chỉnh")

local SliderWS = Tabs.Setting:AddSlider("WalkSpeed", {
    Title = "Tốc Độ Chạy",
    Default = 16,
    Min = 16,
    Max = 200,
    Rounding = 1,
})

SliderWS:OnChanged(function(value)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = value
end)

local SliderJP = Tabs.Setting:AddSlider("JumpPower", {
    Title = "Sức Nhảy", 
    Default = 50,
    Min = 50,
    Max = 200,
    Rounding = 1,
})

SliderJP:OnChanged(function(value)
    game.Players.LocalPlayer.Character.Humanoid.JumpPower = value
end)

-- Toggle Anti AFK
Tabs.Setting:AddToggle("AntiAFK", {
    Title = "Chống AFK",
    Default = true,
})

-- Icon Button
local toggleGui = Instance.new("ScreenGui", game.CoreGui)
local toggleBtn = Instance.new("ImageButton", toggleGui)
toggleBtn.Size = UDim2.new(0, 50, 0, 50)
toggleBtn.Position = UDim2.new(0, 10, 0, 10)
toggleBtn.Image = "http://www.roblox.com/asset/?id=77817527501053"
toggleBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
toggleBtn.ImageColor3 = Color3.fromRGB(255, 215, 0)

local btnCorner = Instance.new("UICorner", toggleBtn)
btnCorner.CornerRadius = UDim.new(0, 12)

-- Particle Effect
local particles = Instance.new("ParticleEmitter", toggleBtn)
particles.Color = ColorSequence.new(Color3.fromRGB(255, 215, 0), Color3.fromRGB(255, 165, 0))
particles.Lifetime = NumberRange.new(0.5, 1)
particles.Rate = 0
particles.Speed = NumberRange.new(5, 10)

toggleBtn.MouseButton1Click:Connect(function()
    particles.Rate = 100
    task.delay(1, function()
        particles.Rate = 0
    end)
    game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.F9, false, game)
end)

-- Thông báo hoàn tất
Fluent:Notify({
    Title = "🎊 gà gaming Hub VIP",
    Content = "Giao diện cao cấp đã sẵn sàng!",
    Duration = 5
})

-- Auto Clicker đơn giản
spawn(function()
    while task.wait() do
        if _G.AutoLevel then
            pcall(function()
                -- Code farm level đơn giản
                local enemies = game:GetService("Workspace").Enemies:GetChildren()
                for _, enemy in pairs(enemies) do
                    if enemy:FindFirstChild("Humanoid") and enemy.Humanoid.Health > 0 then
                        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame
                        break
                    end
                end
            end)
        end
    end
end)