-- [[ GÀ GAMING HUB - PHIÊN BẢN KAVO UI - HIỆN MENU 100% ]] --
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("🔥 GÀ GAMING ULTIMATE | CẤP 2754 🔥", "Midnight")

-- [[ BIẾN ĐIỀU KHIỂN ]] --
_G.AutoFarm = false
_G.AutoBone = false
_G.SelectWeapon = "Melee"

-- [[ TẠO TAB ]] --
local MainTab = Window:NewTab("Gà Farm")
local MainSection = MainTab:NewSection("Hệ Thống Farm Siêu Cấp")

MainSection:NewToggle("Auto Farm Level", "Vừa bay vừa vả quái", function(state)
    _G.AutoFarm = state
end)

MainSection:NewToggle("Auto Farm Xương (Bone)", "Farm tại Haunted Castle", function(state)
    _G.AutoBone = state
end)

MainSection:NewDropdown("Chọn Vũ Khí", "Chọn cái vợ đang cầm", {"Melee", "Sword", "Fruit"}, function(currentOption)
    _G.SelectWeapon = currentOption
end)

-- [[ HÀM ĐÁNH SIÊU TỐC SIÊU FIX ]] --
local function GaGamingSuperAttack()
    pcall(function()
        local Net = game:GetService("ReplicatedStorage").Modules.Net
        Net.RegisterAttack:FireServer()
        for _, v in pairs(game:GetService("Workspace").Enemies:GetChildren()) do
            if v:FindFirstChild("HumanoidRootPart") and (v.HumanoidRootPart.Position - game.Players.LocalPlayer.Character.HumanoidRootPart.Position).Magnitude < 50 then
                Net.RegisterHit:FireServer(v.HumanoidRootPart)
            end
        end
    end)
end

-- [[ VÒNG LẶP FARM CHÍNH ]] --
spawn(function()
    while task.wait() do
        if _G.AutoFarm or _G.AutoBone then
            pcall(function()
                local char = game.Players.LocalPlayer.Character
                local root = char.HumanoidRootPart
                
                -- Tìm quái (Ưu tiên quái gần nhất hoặc theo tên)
                local target = nil
                for _, v in pairs(game:GetService("Workspace").Enemies:GetChildren()) do
                    if v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 then
                        target = v break
                    end
                end

                if target then
                    -- FIX LỖI ĐỨNG YÊN: Ép nhân vật đứng sát quái (độ cao 3 studs)
                    root.CFrame = target.HumanoidRootPart.CFrame * CFrame.new(0, 3, 0)
                    
                    -- Gom quái (Bring Mob)
                    target.HumanoidRootPart.CanCollide = false
                    target.HumanoidRootPart.CFrame = root.CFrame * CFrame.new(0, -3, 0)

                    -- Cầm vũ khí
                    for _, tool in pairs(game.Players.LocalPlayer.Backpack:GetChildren()) do
                        if tool.ToolTip == _G.SelectWeapon then
                            char.Humanoid:EquipTool(tool)
                        end
                    end
                    
                    -- VẢ QUÁI
                    GaGamingSuperAttack()
                end
            end)
        end
    end
end)

-- Nút ẩn hiện cho điện thoại
local SettingsTab = Window:NewTab("Cài đặt")
local SettingsSection = SettingsTab:NewSection("Phím tắt")
SettingsSection:NewKeybind("Ẩn/Hiện Menu", "Bấm phím này để ẩn", Enum.KeyCode.RightControl, function()
	Library:ToggleLib()
end)
