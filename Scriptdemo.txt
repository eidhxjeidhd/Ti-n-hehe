-- [[ GÀ GAMING HUB - BẢN FULL FIX LỖI KHÔNG ĐÁNH ]] --
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "🔥 GÀ GAMING HUB | FIX ALL BUG 🔥",
   LoadingTitle = "Đang kết nối dữ liệu siêu cấp...",
   ConfigurationSaving = { Enabled = true, FileName = "GaGamingFull" }
})

-- [[ BIẾN ĐIỀU KHIỂN ]] --
_G.AutoFarm = false
_G.AutoBone = false
_G.BringMob = false
_G.FastAttack = true
_G.SelectWeapon = "Melee"

-- [[ TAB CHÍNH ]] --
local MainTab = Window:CreateTab("Gà Farm", 4483362458)

MainTab:CreateSection("Hệ Thống Farm Siêu Cấp")

MainTab:CreateToggle({
   Name = "Auto Farm Level (Max 2800)",
   CurrentValue = false,
   Callback = function(Value) 
      _G.AutoFarm = Value 
      _G.BringMob = Value 
   end,
})

MainTab:CreateToggle({
   Name = "Auto Farm Xương (Haunted Castle)",
   CurrentValue = false,
   Callback = function(Value) 
      _G.AutoBone = Value 
      _G.BringMob = Value 
   end,
})

MainTab:CreateDropdown({
   Name = "Chọn Vũ Khí",
   Options = {"Melee","Sword","Fruit"},
   CurrentOption = {"Melee"},
   Callback = function(Option) _G.SelectWeapon = Option[1] end,
})

-- [[ HÀM ĐÁNH TRỰC TIẾP (FIX LỖI) ]] --
local function GàGamingAttack()
    pcall(function()
        local Net = game:GetService("ReplicatedStorage").Modules.Net
        Net.RegisterAttack:FireServer()
        local monster = game:GetService("Workspace").Enemies:FindFirstChildOfClass("Model")
        if monster and monster:FindFirstChild("HumanoidRootPart") then
            Net.RegisterHit:FireServer(monster.HumanoidRootPart)
        end
    end)
end

-- [[ VÒNG LẶP XỬ LÝ TẤT CẢ TÍNH NĂNG ]] --
spawn(function()
    while task.wait() do
        if _G.AutoFarm or _G.AutoBone then
            pcall(function()
                local char = game.Players.LocalPlayer.Character
                local root = char.HumanoidRootPart
                
                -- Tìm quái theo chế độ farm
                local target = nil
                if _G.AutoBone then
                    for _, v in pairs(game:GetService("Workspace").Enemies:GetChildren()) do
                        if (v.Name == "Reborn Skeleton" or v.Name == "Living Zombie") and v.Humanoid.Health > 0 then
                            target = v break
                        end
                    end
                else
                    target = game:GetService("Workspace").Enemies:FindFirstChildOfClass("Model")
                end

                if target and target:FindFirstChild("HumanoidRootPart") then
                    -- Bay sát quái
                    root.CFrame = target.HumanoidRootPart.CFrame * CFrame.new(0, 6, 0)
                    
                    -- Gom quái xung quanh
                    if _G.BringMob then
                        for _, m in pairs(game:GetService("Workspace").Enemies:GetChildren()) do
                            if m:FindFirstChild("HumanoidRootPart") and (m.HumanoidRootPart.Position - root.Position).Magnitude < 250 then
                                m.HumanoidRootPart.CFrame = root.CFrame * CFrame.new(0, -6, 0)
                                m.HumanoidRootPart.CanCollide = false
                            end
                        end
                    end

                    -- Cầm vũ khí
                    for _, tool in pairs(game.Players.LocalPlayer.Backpack:GetChildren()) do
                        if tool.ToolTip == _G.SelectWeapon then
                            char.Humanoid:EquipTool(tool)
                        end
                    end
                    
                    -- Thực hiện đánh siêu tốc
                    GàGamingAttack()
                end
            end)
        end
    end
end)

-- [[ CHỐNG AFK ]] --
game:GetService("Players").LocalPlayer.Idled:Connect(function()
    game:GetService("VirtualUser"):Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
    wait(1)
    game:GetService("VirtualUser"):Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
end)