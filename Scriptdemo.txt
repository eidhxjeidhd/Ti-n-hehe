-- [[ GÀ GAMING HUB - BẢN FULL KHÔNG MẤT TÍNH NĂNG ]] --
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "🔥 GÀ GAMING HUB | BẢN CUỐI CỰC MƯỢT 🔥",
   LoadingTitle = "Đang gộp tất cả tính năng cho vợ yêu...",
   ConfigurationSaving = { Enabled = true, FileName = "GaGamingFullFinal" }
})

-- [[ BIẾN ĐIỀU KHIỂN ]] --
_G.AutoFarm = false
_G.AutoBone = false
_G.AutoDeathKing = false
_G.BringMob = false
_G.FastAttack = true
_G.SelectWeapon = "Melee"

-- [[ TAB CHÍNH ]] --
local MainTab = Window:CreateTab("Gà Farm", 4483362458)

MainTab:CreateSection("Hệ Thống GÀ GAMING")

MainTab:CreateToggle({
   Name = "Auto Farm Level (Max 2800)",
   CurrentValue = false,
   Callback = function(Value) 
      _G.AutoFarm = Value 
      _G.BringMob = Value 
   end,
})

MainTab:CreateToggle({
   Name = "Auto Farm Xương (Haunted Castle)",
   CurrentValue = false,
   Callback = function(Value) 
      _G.AutoBone = Value 
      _G.BringMob = Value 
   end,
})

MainTab:CreateToggle({
   Name = "Tự Đổi Xương (Death King)",
   CurrentValue = false,
   Callback = function(Value) 
      _G.AutoDeathKing = Value 
   end,
})

MainTab:CreateDropdown({
   Name = "Vũ khí sử dụng",
   Options = {"Melee","Sword","Fruit"},
   CurrentOption = {"Melee"},
   Callback = function(Option) _G.SelectWeapon = Option[1] end,
})

-- [[ HÀM ĐÁNH FIX LỖI (DO ATTACK) ]] --
local function GàGamingAttack()
    pcall(function()
        local Net = game:GetService("ReplicatedStorage").Modules.Net
        Net.RegisterAttack:FireServer() -- Lệnh vung tay
        local monster = game:GetService("Workspace").Enemies:FindFirstChildOfClass("Model")
        if monster and monster:FindFirstChild("HumanoidRootPart") then
            Net.RegisterHit:FireServer(monster.HumanoidRootPart) -- Lệnh gây sát thương
        end
    end)
end

-- [[ VÒNG LẶP XỬ LÝ TẤT CẢ ]] --
spawn(function()
    while task.wait() do
        if _G.AutoFarm or _G.AutoBone then
            pcall(function()
                local char = game.Players.LocalPlayer.Character
                local root = char.HumanoidRootPart
                
                -- Tìm quái mục tiêu
                local target = nil
                if _G.AutoBone then
                    for _, v in pairs(game:GetService("Workspace").Enemies:GetChildren()) do
                        if (v.Name == "Reborn Skeleton" or v.Name == "Living Zombie" or v.Name == "Demonic Soul") and v.Humanoid.Health > 0 then
                            target = v break
                        end
                    end
                else
                    target = game:GetService("Workspace").Enemies:FindFirstChildOfClass("Model")
                end

                if target and target:FindFirstChild("HumanoidRootPart") then
                    -- BAY SÁT QUÁI (Hạ độ cao xuống 4 để đánh trúng)
                    root.CFrame = target.HumanoidRootPart.CFrame * CFrame.new(0, 4, 0)
                    
                    -- Gom quái (Bring Mob)
                    if _G.BringMob then
                        for _, m in pairs(game:GetService("Workspace").Enemies:GetChildren()) do
                            if m:FindFirstChild("HumanoidRootPart") and (m.HumanoidRootPart.Position - root.Position).Magnitude < 250 then
                                m.HumanoidRootPart.CFrame = root.CFrame * CFrame.new(0, -4, 0)
                                m.HumanoidRootPart.CanCollide = false
                            end
                        end
                    end

                    -- Tự động cầm vũ khí
                    for _, tool in pairs(game.Players.LocalPlayer.Backpack:GetChildren()) do
                        if tool.ToolTip == _G.SelectWeapon then
                            char.Humanoid:EquipTool(tool)
                        end
                    end
                    
                    -- ĐÁNH
                    GàGamingAttack()
                end
            end)
        end
        
        -- Auto đổi xương tại Death King
        if _G.AutoDeathKing then
            game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("DeathKing","Buy",1)
        end
    end
end)